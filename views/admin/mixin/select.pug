mixin selectTree(categorys, level = 1)
  each item in categorys
    -const prefix = Array(level + 1).join("-- ")
    option(value=item.id) #{prefix}#{item.title}
      if (item.children && item.children.length > 0)
        +selectTree(item.children, level + 1)